@model IEnumerable<QLTHPT.Models.THU>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.THU_TEN)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.THU_TEN)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.THU_MA }) |
            @Html.ActionLink("Details", "Details", new { id=item.THU_MA }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.THU_MA })
        </td>
    </tr>
}

</table>
