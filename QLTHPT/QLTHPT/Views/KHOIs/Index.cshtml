@model IEnumerable<QLTHPT.Models.KHOI>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.KHOI_TEN)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.KHOI_TEN)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.KHOI_MA }) |
            @Html.ActionLink("Details", "Details", new { id=item.KHOI_MA }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.KHOI_MA })
        </td>
    </tr>
}

</table>
